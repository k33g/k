import monet from 'monet';

/**
 * This is a helper to read command line arguments
 * and load the appropriate module
 */
export default class Module {

  /**
   * @returns {Maybe} value of Maybe is the name of the module or None if error
   */
  name() {
    return null
  }
}